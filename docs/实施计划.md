# 微信小程序产品展示 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个科技消费品展示微信小程序，纯展示目录，用户浏览后一键复制微信号联系客服。

**Architecture:** 微信原生 WXML + WXSS + JS 前端，5 个页面（首页/分类/搜索/详情/关于），1 个产品卡片组件。数据层使用微信云开发的云数据库存储产品数据和配置，2 个云函数提供查询和搜索接口。

**Tech Stack:** 微信小程序原生框架、微信云开发（云数据库 + 云函数 + 云存储）、微信开发者工具

---

## 文件结构

```
miniprogram/
├── project.config.json          # 项目配置
├── app.js                       # 入口，云开发初始化
├── app.json                     # 全局配置（路由/tabBar/window）
├── app.wxss                     # 全局样式（Dark Tech 主题变量）
├── pages/
│   ├── index/                   # 首页：Banner + 分类入口 + 热门推荐
│   │   ├── index.js
│   │   ├── index.json
│   │   ├── index.wxml
│   │   └── index.wxss
│   ├── category/                # 分类列表页：产品网格 + 分页
│   │   ├── category.js
│   │   ├── category.json
│   │   ├── category.wxml
│   │   └── category.wxss
│   ├── search/                  # 搜索页：输入即搜 + 热门标签
│   │   ├── search.js
│   │   ├── search.json
│   │   ├── search.wxml
│   │   └── search.wxss
│   ├── detail/                  # 产品详情：轮播 + 规格 + 复制微信号
│   │   ├── detail.js
│   │   ├── detail.json
│   │   ├── detail.wxml
│   │   └── detail.wxss
│   └── about/                   # 关于页：品牌信息 + 产品统计
│       ├── about.js
│       ├── about.json
│       ├── about.wxml
│       └── about.wxss
├── components/
│   └── product-card/            # 产品卡片组件（列表/网格复用）
│       ├── product-card.js
│       ├── product-card.json
│       ├── product-card.wxml
│       └── product-card.wxss
├── utils/
│   └── util.js                  # 工具函数（复制微信号、价格格式化）
└── cloudfunctions/
    ├── getProducts/             # 云函数：按分类/热门/分页查询
    │   ├── index.js
    │   └── package.json
    └── searchProducts/          # 云函数：关键词搜索
        ├── index.js
        └── package.json
```

---

### Task 1: 注册微信小程序账号

**目标:** 获得小程序 AppID，开通云开发。

- [ ] **Step 1: 注册小程序账号**

访问 https://mp.weixin.qq.com ，点击「立即注册」→「小程序」。
- 类型选「企业」或「个体工商户」（需营业执照）
- 如果没有营业执照，先用测试号开发：微信开发者工具 → 创建项目 → 选择「测试号」

- [ ] **Step 2: 获取 AppID**

登录小程序后台 → 开发管理 → 开发设置 → 复制 AppID（如 `wxXXXXXXXXXXXXXXXX`）。

- [ ] **Step 3: 开通云开发**

小程序后台 → 开发 → 云开发 → 开通 → 创建环境（环境名如 `product-catalog`）。
选择「按量付费」，免费额度内无需充值。

---

### Task 2: 创建小程序项目并初始化云开发

**Files:**
- Create: `miniprogram/project.config.json`
- Create: `miniprogram/app.js`
- Create: `miniprogram/app.json`
- Create: `miniprogram/app.wxss`

- [ ] **Step 1: 用微信开发者工具创建项目**

打开微信开发者工具 → 新建项目 →
- 项目名称：`mini-product`
- 目录：选择 `miniprogram/` 父目录
- AppID：填入 Task 1 获得的 AppID
- 开发模式：小程序
- 后端服务：微信云开发

- [ ] **Step 2: 配置 project.config.json**

在开发者工具中点击「云开发」图标，工具会自动在 `project.config.json` 中注入 `cloudfunctionRoot` 配置。确认文件包含：

```json
{
  "miniprogramRoot": "",
  "cloudfunctionRoot": "cloudfunctions/",
  "setting": {
    "urlCheck": true,
    "es6": true,
    "enhance": true,
    "postcss": true,
    "minified": true
  }
}
```

- [ ] **Step 3: 编写 app.js — 云开发初始化**

```js
// app.js
App({
  onLaunch() {
    wx.cloud.init({
      env: 'product-catalog', // 替换为你的云环境 ID
      traceUser: true
    });
  }
});
```

- [ ] **Step 4: 编写 app.json — 全局路由和 tabBar**

```json
{
  "pages": [
    "pages/index/index",
    "pages/category/category",
    "pages/search/search",
    "pages/detail/detail",
    "pages/about/about"
  ],
  "window": {
    "navigationBarBackgroundColor": "#0a0a0f",
    "navigationBarTitleText": "产品目录",
    "navigationBarTextStyle": "white",
    "backgroundColor": "#0a0a0f"
  },
  "tabBar": {
    "color": "#9e9e9e",
    "selectedColor": "#00e5ff",
    "backgroundColor": "#0a0a0f",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页"
      },
      {
        "pagePath": "pages/category/category",
        "text": "分类"
      },
      {
        "pagePath": "pages/about/about",
        "text": "关于"
      }
    ]
  },
  "style": "v2",
  "sitemapLocation": "sitemap.json"
}
```

- [ ] **Step 5: 编写 app.wxss — 全局主题变量**

```css
/* Dark Tech 全局主题 */
page {
  --bg-primary: #0a0a0f;
  --bg-card: #14141f;
  --text-primary: #f5f5f5;
  --text-secondary: #9e9e9e;
  --accent: #00e5ff;
  --accent-warm: #ff6d00;
  --divider: #1e1e2e;

  background-color: var(--bg-primary);
  color: var(--text-primary);
  font-size: 14px;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
```

- [ ] **Step 6: 验证**

在开发者工具中点击「编译」，应看到空白深色页面，底部三个 tab 可切换。Console 无报错。

---

### Task 3: 创建云数据库集合并导入示例数据

- [ ] **Step 1: 创建 products 集合**

微信开发者工具 → 云开发控制台 → 数据库 → 添加集合 → 名称 `products`。

- [ ] **Step 2: 设置 products 集合权限**

集合 → 权限设置 → 选择「所有用户可读，仅创建者可写」。

- [ ] **Step 3: 创建 config 集合**

同上，添加集合 `config`，权限设为「所有用户可读，仅创建者可写」。

- [ ] **Step 4: 导入 config 数据**

在 `config` 集合中点击「添加记录」：

```json
{
  "_id": "contact",
  "wechatId": "你的微信号"
}
```

- [ ] **Step 5: 导入示例产品数据**

在 `products` 集合中，点击「添加记录」，逐条导入 3 条示例数据（用于开发调试）：

```json
{
  "name": "Pro X1 降噪耳机",
  "price": 299,
  "brand": "声阔",
  "model": "X1",
  "category": "耳机",
  "description": "40dB主动降噪，60小时续航，Hi-Res金标认证",
  "specs": [
    { "key": "驱动单元", "value": "40mm" },
    { "key": "频率响应", "value": "20Hz-20kHz" },
    { "key": "连接方式", "value": "蓝牙5.3 / 有线" },
    { "key": "续航", "value": "60小时" }
  ],
  "images": [],
  "isHot": true,
  "sort": 100,
  "createdAt": "2026-06-14T00:00:00Z"
}
```

```json
{
  "name": "G Pro 无线游戏鼠标",
  "price": 199,
  "brand": "雷蛇",
  "model": "G Pro",
  "category": "鼠标",
  "description": "轻量级68g，26000DPI光学传感器，70小时续航",
  "specs": [
    { "key": "传感器", "value": "Focus Pro 26K" },
    { "key": "DPI", "value": "26000" },
    { "key": "重量", "value": "68g" },
    { "key": "连接方式", "value": "2.4G无线 / 蓝牙" }
  ],
  "images": [],
  "isHot": true,
  "sort": 90,
  "createdAt": "2026-06-14T00:00:00Z"
}
```

```json
{
  "name": "K3 机械键盘",
  "price": 349,
  "brand": "Keychron",
  "model": "K3",
  "category": "键盘",
  "description": "75%配列，矮轴热插拔，RGB背光，Mac/Win双系统",
  "specs": [
    { "key": "配列", "value": "75% (84键)" },
    { "key": "轴体", "value": "佳达隆矮轴 茶轴" },
    { "key": "背光", "value": "RGB 18种灯效" },
    { "key": "连接方式", "value": "蓝牙5.1 / Type-C有线" }
  ],
  "images": [],
  "isHot": false,
  "sort": 80,
  "createdAt": "2026-06-14T00:00:00Z"
}
```

- [ ] **Step 6: 验证**

在云开发控制台数据库页面，确认两个集合都有记录，数据可正常查看。

---

### Task 4: 云函数 — getProducts

**Files:**
- Create: `cloudfunctions/getProducts/package.json`
- Create: `cloudfunctions/getProducts/index.js`

- [ ] **Step 1: 创建云函数**

微信开发者工具 → 云开发目录右键 `cloudfunctions/` → 新建 Node.js 云函数 → 输入 `getProducts`。

或在终端执行：
```bash
mkdir -p cloudfunctions/getProducts
```

- [ ] **Step 2: 编写 package.json**

```json
{
  "name": "getProducts",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
```

- [ ] **Step 3: 编写 index.js**

```js
// cloudfunctions/getProducts/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event) => {
  const { category, isHot, page = 1, pageSize = 10 } = event;
  const _ = db.command;
  const where = {};

  if (category) {
    where.category = category;
  }
  if (isHot !== undefined) {
    where.isHot = isHot;
  }

  const skip = (page - 1) * pageSize;

  try {
    const countResult = await db.collection('products').where(where).count();
    const total = countResult.total;

    const { data } = await db.collection('products')
      .where(where)
      .orderBy('sort', 'desc')
      .skip(skip)
      .limit(pageSize)
      .get();

    return {
      success: true,
      data,
      total,
      page,
      pageSize,
      hasMore: skip + data.length < total
    };
  } catch (err) {
    return {
      success: false,
      error: err.message
    };
  }
};
```

- [ ] **Step 4: 安装依赖并上传**

右键 `getProducts` 文件夹 →「上传并部署：云端安装依赖」。

- [ ] **Step 5: 验证**

在云开发控制台 → 云函数 → `getProducts` → 测试，参数填：
```json
{ "isHot": true, "page": 1, "pageSize": 10 }
```
预期返回 2 条 `isHot: true` 的产品数据。

---

### Task 5: 云函数 — searchProducts

**Files:**
- Create: `cloudfunctions/searchProducts/package.json`
- Create: `cloudfunctions/searchProducts/index.js`

- [ ] **Step 1: 创建云函数目录**

```bash
mkdir -p cloudfunctions/searchProducts
```

- [ ] **Step 2: 编写 package.json**

```json
{
  "name": "searchProducts",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
```

- [ ] **Step 3: 编写 index.js**

```js
// cloudfunctions/searchProducts/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event) => {
  const { keyword = '', page = 1, pageSize = 20 } = event;
  const trimKey = keyword.trim();

  if (!trimKey) {
    return { success: true, data: [], total: 0 };
  }

  const _ = db.command;
  const regex = db.RegExp({
    regexp: trimKey,
    options: 'i'
  });

  const skip = (page - 1) * pageSize;

  try {
    const countResult = await db.collection('products')
      .where(_.or([{ name: regex }, { description: regex }]))
      .count();

    const { data } = await db.collection('products')
      .where(_.or([{ name: regex }, { description: regex }]))
      .orderBy('sort', 'desc')
      .skip(skip)
      .limit(pageSize)
      .get();

    return {
      success: true,
      data,
      total: countResult.total,
      keyword: trimKey
    };
  } catch (err) {
    return {
      success: false,
      error: err.message
    };
  }
};
```

- [ ] **Step 4: 安装依赖并上传**

右键 `searchProducts` 文件夹 →「上传并部署：云端安装依赖」。

- [ ] **Step 5: 验证**

在云开发控制台测试云函数，参数填：
```json
{ "keyword": "耳机" }
```
预期返回 1 条包含"耳机"的产品。

---

### Task 6: 工具函数 util.js

**Files:**
- Create: `miniprogram/utils/util.js`

- [ ] **Step 1: 编写 util.js**

```js
// utils/util.js

/**
 * 复制微信号到剪贴板
 */
function copyWechatId(wechatId) {
  wx.setClipboardData({
    data: wechatId,
    success() {
      wx.showToast({
        title: '微信号已复制',
        icon: 'success',
        duration: 2000
      });
    }
  });
}

/**
 * 价格格式化：299 → ¥299
 */
function formatPrice(price) {
  return '¥' + price;
}

/**
 * 获取配置表数据
 */
async function fetchConfig(db, key) {
  try {
    const { data } = await db.collection('config').doc(key).get();
    return data;
  } catch (err) {
    return null;
  }
}

module.exports = {
  copyWechatId,
  formatPrice,
  fetchConfig
};
```

---

### Task 7: 产品卡片组件 product-card

**Files:**
- Create: `miniprogram/components/product-card/product-card.js`
- Create: `miniprogram/components/product-card/product-card.json`
- Create: `miniprogram/components/product-card/product-card.wxml`
- Create: `miniprogram/components/product-card/product-card.wxss`

- [ ] **Step 1: 创建组件目录**

```bash
mkdir -p miniprogram/components/product-card
```

- [ ] **Step 2: 编写 product-card.json**

```json
{
  "component": true,
  "usingComponents": {}
}
```

- [ ] **Step 3: 编写 product-card.js**

```js
// components/product-card/product-card.js
const util = require('../../utils/util');

Component({
  properties: {
    product: {
      type: Object,
      value: {}
    }
  },

  data: {
    displayImage: ''
  },

  lifetimes: {
    attached() {
      this.setDisplayImage();
    }
  },

  observers: {
    'product'() {
      this.setDisplayImage();
    }
  },

  methods: {
    setDisplayImage() {
      const product = this.properties.product;
      if (product.images && product.images.length > 0) {
        this.setData({ displayImage: product.images[0] });
      } else {
        this.setData({ displayImage: '' });
      }
    },

    formatPrice(price) {
      return util.formatPrice(price);
    },

    onTap() {
      this.triggerEvent('tap', { product: this.properties.product });
    }
  }
});
```

- [ ] **Step 4: 编写 product-card.wxml**

```html
<!-- components/product-card/product-card.wxml -->
<view class="card" bindtap="onTap">
  <image
    wx:if="{{displayImage}}"
    class="card-image"
    src="{{displayImage}}"
    mode="aspectFill"
    lazy-load
  />
  <view wx:else class="card-placeholder">
    <text class="placeholder-icon">📦</text>
  </view>
  <view class="card-body">
    <text class="card-name">{{product.name}}</text>
    <text class="card-price">{{formatPrice(product.price)}}</text>
  </view>
</view>
```

- [ ] **Step 5: 编写 product-card.wxss**

```css
/* components/product-card/product-card.wxss */
.card {
  background-color: var(--bg-card);
  border-radius: 8rpx;
  overflow: hidden;
  margin: 8rpx;
}

.card-image {
  width: 100%;
  height: 340rpx;
  background-color: #1a1a2e;
}

.card-placeholder {
  width: 100%;
  height: 340rpx;
  background-color: #1a1a2e;
  display: flex;
  align-items: center;
  justify-content: center;
}

.placeholder-icon {
  font-size: 64rpx;
}

.card-body {
  padding: 20rpx 16rpx;
}

.card-name {
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  font-size: 28rpx;
  color: var(--text-primary);
  line-height: 1.4;
}

.card-price {
  display: block;
  margin-top: 12rpx;
  font-size: 32rpx;
  font-weight: 700;
  color: var(--accent);
}
```

---

### Task 8: 首页 pages/index

**Files:**
- Create: `miniprogram/pages/index/index.js`
- Create: `miniprogram/pages/index/index.json`
- Create: `miniprogram/pages/index/index.wxml`
- Create: `miniprogram/pages/index/index.wxss`

- [ ] **Step 1: 创建页面目录**

```bash
mkdir -p miniprogram/pages/index
```

- [ ] **Step 2: 编写 index.json**

```json
{
  "usingComponents": {
    "product-card": "/components/product-card/product-card"
  },
  "enablePullDownRefresh": true
}
```

- [ ] **Step 3: 编写 index.js**

```js
// pages/index/index.js
const util = require('../../utils/util');
const db = wx.cloud.database();

Page({
  data: {
    banners: [],       // 热门产品用于轮播
    categories: [
      { name: '耳机', icon: '🎧' },
      { name: '鼠标', icon: '🖱️' },
      { name: '键盘', icon: '⌨️' },
      { name: '配件', icon: '🔌' }
    ],
    hotProducts: [],
    wechatId: ''
  },

  onLoad() {
    this.loadData();
    this.loadConfig();
  },

  onPullDownRefresh() {
    this.loadData().then(() => wx.stopPullDownRefresh());
  },

  async loadData() {
    try {
      const res = await wx.cloud.callFunction({
        name: 'getProducts',
        data: { isHot: true, page: 1, pageSize: 4 }
      });

      if (res.result.success) {
        const hotProducts = res.result.data;
        this.setData({
          banners: hotProducts.slice(0, 3),
          hotProducts
        });
      }
    } catch (err) {
      wx.showToast({ title: '加载失败', icon: 'none' });
    }
  },

  async loadConfig() {
    const config = await util.fetchConfig(db, 'contact');
    if (config) {
      this.setData({ wechatId: config.wechatId });
    }
  },

  onTapSearch() {
    wx.navigateTo({ url: '/pages/search/search' });
  },

  onTapCategory(e) {
    const name = e.currentTarget.dataset.name;
    wx.switchTab({ url: '/pages/category/category' });
    // 存储选中分类供 category 页读取
    wx.setStorageSync('activeCategory', name);
  },

  onTapProduct(e) {
    const product = e.detail.product;
    wx.navigateTo({
      url: `/pages/detail/detail?id=${product._id}`
    });
  },

  onTapBanner(e) {
    const product = e.currentTarget.dataset.product;
    wx.navigateTo({
      url: `/pages/detail/detail?id=${product._id}`
    });
  },

  onCopyWechat() {
    util.copyWechatId(this.data.wechatId);
  }
});
```

- [ ] **Step 4: 编写 index.wxml**

```html
<!-- pages/index/index.wxml -->
<view class="container">

  <!-- 搜索栏 -->
  <view class="search-bar" bindtap="onTapSearch">
    <text class="search-icon">🔍</text>
    <text class="search-placeholder">搜索产品</text>
  </view>

  <!-- 轮播 Banner -->
  <swiper
    wx:if="{{banners.length}}"
    class="banner-swiper"
    indicator-dots
    indicator-color="#9e9e9e"
    indicator-active-color="#00e5ff"
    autoplay
    circular
    interval="4000"
  >
    <swiper-item
      wx:for="{{banners}}"
      wx:key="_id"
      data-product="{{item}}"
      bindtap="onTapBanner"
    >
      <image
        wx:if="{{item.images[0]}}"
        src="{{item.images[0]}}"
        mode="aspectFill"
        class="banner-image"
      />
      <view class="banner-overlay">
        <text class="banner-name">{{item.name}}</text>
        <text class="banner-price">¥{{item.price}}</text>
      </view>
    </swiper-item>
  </swiper>

  <!-- 分类入口 -->
  <view class="category-grid">
    <view
      wx:for="{{categories}}"
      wx:key="name"
      class="category-item"
      data-name="{{item.name}}"
      bindtap="onTapCategory"
    >
      <text class="category-icon">{{item.icon}}</text>
      <text class="category-name">{{item.name}}</text>
    </view>
  </view>

  <!-- 热门推荐 -->
  <view class="section">
    <text class="section-title">热门推荐</text>
    <view class="product-grid">
      <product-card
        wx:for="{{hotProducts}}"
        wx:key="_id"
        product="{{item}}"
        bindtap="onTapProduct"
      />
    </view>
  </view>

</view>
```

- [ ] **Step 5: 编写 index.wxss**

```css
/* pages/index/index.wxss */
.container {
  padding-bottom: 40rpx;
}

/* 搜索栏 */
.search-bar {
  display: flex;
  align-items: center;
  margin: 24rpx 32rpx;
  padding: 20rpx 24rpx;
  background-color: var(--bg-card);
  border-radius: 16rpx;
}
.search-icon {
  font-size: 28rpx;
  margin-right: 16rpx;
}
.search-placeholder {
  color: var(--text-secondary);
  font-size: 28rpx;
}

/* 轮播 Banner */
.banner-swiper {
  width: 100%;
  height: 360rpx;
}
.banner-image {
  width: 100%;
  height: 100%;
}
.banner-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 40rpx 32rpx 24rpx;
  background: linear-gradient(transparent, rgba(10, 10, 15, 0.9));
}
.banner-name {
  color: var(--text-primary);
  font-size: 32rpx;
  font-weight: 600;
  display: block;
}
.banner-price {
  color: var(--accent);
  font-size: 28rpx;
  font-weight: 700;
  margin-top: 8rpx;
  display: block;
}

/* 分类入口 */
.category-grid {
  display: flex;
  justify-content: space-around;
  padding: 32rpx 24rpx;
}
.category-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}
.category-icon {
  font-size: 48rpx;
  margin-bottom: 8rpx;
}
.category-name {
  color: var(--text-secondary);
  font-size: 24rpx;
}

/* 推荐区域 */
.section {
  padding: 0 24rpx;
}
.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 16rpx;
  padding-left: 8rpx;
}
.product-grid {
  display: flex;
  flex-wrap: wrap;
  margin: -8rpx;
}
.product-grid > product-card {
  width: 50%;
}
```

- [ ] **Step 6: 验证**

编译运行，首页显示轮播、分类入口、热门推荐。点击分类跳转到分类页，点击搜索跳搜索页。

---

### Task 9: 分类列表页 pages/category

**Files:**
- Create: `miniprogram/pages/category/category.js`
- Create: `miniprogram/pages/category/category.json`
- Create: `miniprogram/pages/category/category.wxml`
- Create: `miniprogram/pages/category/category.wxss`

- [ ] **Step 1: 编写 category.json**

```json
{
  "usingComponents": {
    "product-card": "/components/product-card/product-card"
  },
  "enablePullDownRefresh": true
}
```

- [ ] **Step 2: 编写 category.js**

```js
// pages/category/category.js
Page({
  data: {
    categories: ['耳机', '鼠标', '键盘', '配件'],
    activeCategory: '耳机',
    products: [],
    total: 0,
    page: 1,
    pageSize: 10,
    hasMore: true,
    loading: false
  },

  onLoad() {
    const stored = wx.getStorageSync('activeCategory');
    if (stored && this.data.categories.includes(stored)) {
      this.setData({ activeCategory: stored });
      wx.removeStorageSync('activeCategory');
    }
    this.loadProducts();
  },

  onShow() {
    const stored = wx.getStorageSync('activeCategory');
    if (stored && this.data.categories.includes(stored) && stored !== this.data.activeCategory) {
      this.setData({ activeCategory: stored, products: [], page: 1, hasMore: true });
      wx.removeStorageSync('activeCategory');
      this.loadProducts();
    }
  },

  onPullDownRefresh() {
    this.setData({ products: [], page: 1, hasMore: true });
    this.loadProducts().then(() => wx.stopPullDownRefresh());
  },

  onReachBottom() {
    if (this.data.hasMore && !this.data.loading) {
      this.loadProducts();
    }
  },

  async loadProducts() {
    this.setData({ loading: true });

    try {
      const res = await wx.cloud.callFunction({
        name: 'getProducts',
        data: {
          category: this.data.activeCategory,
          page: this.data.page,
          pageSize: this.data.pageSize
        }
      });

      if (res.result.success) {
        const newList = this.data.products.concat(res.result.data);
        this.setData({
          products: newList,
          total: res.result.total,
          hasMore: res.result.hasMore,
          page: this.data.page + 1,
          loading: false
        });
      }
    } catch (err) {
      wx.showToast({ title: '加载失败', icon: 'none' });
      this.setData({ loading: false });
    }
  },

  onSwitchCategory(e) {
    const name = e.currentTarget.dataset.name;
    if (name !== this.data.activeCategory) {
      this.setData({
        activeCategory: name,
        products: [],
        page: 1,
        hasMore: true
      });
      this.loadProducts();
    }
  },

  onTapProduct(e) {
    const product = e.detail.product;
    wx.navigateTo({
      url: `/pages/detail/detail?id=${product._id}`
    });
  }
});
```

- [ ] **Step 3: 编写 category.wxml**

```html
<!-- pages/category/category.wxml -->
<view class="container">
  <!-- 分类横滑标签 -->
  <scroll-view class="category-tabs" scroll-x>
    <view
      wx:for="{{categories}}"
      wx:key="*this"
      class="tab-item {{activeCategory === item ? 'tab-active' : ''}}"
      data-name="{{item}}"
      bindtap="onSwitchCategory"
    >
      <text>{{item}}</text>
    </view>
  </scroll-view>

  <!-- 产品数量 -->
  <view class="category-info" wx:if="{{total}}">
    <text class="category-count">共 {{total}} 款</text>
  </view>

  <!-- 产品网格 -->
  <view class="product-grid">
    <product-card
      wx:for="{{products}}"
      wx:key="_id"
      product="{{item}}"
      bindtap="onTapProduct"
    />
  </view>

  <!-- 加载状态 -->
  <view class="load-more" wx:if="{{loading}}">
    <text class="load-text">加载中...</text>
  </view>
  <view class="load-more" wx:elif="{{!hasMore && products.length > 0}}">
    <text class="load-text">— 已展示全部 —</text>
  </view>
  <view class="empty" wx:if="{{!loading && products.length === 0}}">
    <text>暂无产品</text>
  </view>
</view>
```

- [ ] **Step 4: 编写 category.wxss**

```css
/* pages/category/category.wxss */
.container {
  padding-bottom: 40rpx;
}

.category-tabs {
  white-space: nowrap;
  padding: 24rpx 24rpx;
  border-bottom: 1rpx solid var(--divider);
}
.tab-item {
  display: inline-block;
  padding: 12rpx 32rpx;
  margin-right: 16rpx;
  border-radius: 32rpx;
  font-size: 28rpx;
  color: var(--text-secondary);
  background-color: var(--bg-card);
  transition: all 0.2s;
}
.tab-active {
  color: #0a0a0f;
  background-color: var(--accent);
  font-weight: 600;
}

.category-info {
  padding: 20rpx 32rpx 8rpx;
}
.category-count {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.product-grid {
  display: flex;
  flex-wrap: wrap;
  padding: 0 16rpx;
}
.product-grid > product-card {
  width: 50%;
}

.load-more {
  text-align: center;
  padding: 32rpx;
}
.load-text {
  color: var(--text-secondary);
  font-size: 24rpx;
}
.empty {
  text-align: center;
  padding: 120rpx 0;
  color: var(--text-secondary);
}
```

---

### Task 10: 搜索页 pages/search

**Files:**
- Create: `miniprogram/pages/search/search.js`
- Create: `miniprogram/pages/search/search.json`
- Create: `miniprogram/pages/search/search.wxml`
- Create: `miniprogram/pages/search/search.wxss`

- [ ] **Step 1: 编写 search.json**

```json
{
  "usingComponents": {},
  "navigationBarTitleText": "搜索"
}
```

- [ ] **Step 2: 编写 search.js**

```js
// pages/search/search.js
Page({
  data: {
    keyword: '',
    results: [],
    hotTags: ['蓝牙', '降噪', '无线', '机械键盘', '游戏鼠标', 'Type-C'],
    searched: false,
    loading: false
  },

  onLoad() {
    // 自动聚焦搜索框
    // input focus 由 wxml focus 属性控制
  },

  onInput(e) {
    const keyword = e.detail.value;
    this.setData({ keyword });

    if (!keyword.trim()) {
      this.setData({ results: [], searched: false });
      return;
    }

    // 防抖：300ms 后执行搜索
    if (this._timer) clearTimeout(this._timer);
    this._timer = setTimeout(() => {
      this.doSearch(keyword.trim());
    }, 300);
  },

  onClear() {
    this.setData({ keyword: '', results: [], searched: false });
  },

  onTapTag(e) {
    const tag = e.currentTarget.dataset.tag;
    this.setData({ keyword: tag });
    this.doSearch(tag);
  },

  async doSearch(keyword) {
    this.setData({ loading: true });

    try {
      const res = await wx.cloud.callFunction({
        name: 'searchProducts',
        data: { keyword }
      });

      if (res.result.success) {
        this.setData({
          results: res.result.data,
          searched: true,
          loading: false
        });
      }
    } catch (err) {
      wx.showToast({ title: '搜索失败', icon: 'none' });
      this.setData({ loading: false });
    }
  },

  onTapResult(e) {
    const product = e.currentTarget.dataset.product;
    wx.navigateTo({
      url: `/pages/detail/detail?id=${product._id}`
    });
  }
});
```

- [ ] **Step 3: 编写 search.wxml**

```html
<!-- pages/search/search.wxml -->
<view class="container">
  <!-- 搜索栏 -->
  <view class="search-bar">
    <text class="search-icon">🔍</text>
    <input
      class="search-input"
      placeholder="搜索产品"
      placeholder-style="color: #9e9e9e"
      value="{{keyword}}"
      focus
      bindinput="onInput"
    />
    <text wx:if="{{keyword}}" class="clear-btn" bindtap="onClear">✕</text>
  </view>

  <!-- 热门搜索（未搜索时） -->
  <view wx:if="{{!searched}}" class="hot-section">
    <text class="hot-title">热门搜索</text>
    <view class="hot-tags">
      <text
        wx:for="{{hotTags}}"
        wx:key="*this"
        class="hot-tag"
        data-tag="{{item}}"
        bindtap="onTapTag"
      >{{item}}</text>
    </view>
  </view>

  <!-- 搜索结果 -->
  <view wx:if="{{searched}}" class="result-section">
    <text class="result-count">找到 {{results.length}} 个产品</text>

    <view
      wx:for="{{results}}"
      wx:key="_id"
      class="result-item"
      data-product="{{item}}"
      bindtap="onTapResult"
    >
      <image
        wx:if="{{item.images[0]}}"
        class="result-image"
        src="{{item.images[0]}}"
        mode="aspectFill"
      />
      <view wx:else class="result-placeholder">📦</view>
      <view class="result-body">
        <text class="result-name">{{item.name}}</text>
        <text class="result-brand">{{item.brand}} · {{item.category}}</text>
        <text class="result-price">¥{{item.price}}</text>
      </view>
    </view>

    <view wx:if="{{results.length === 0 && !loading}}" class="empty">
      <text>未找到相关产品</text>
    </view>
  </view>
</view>
```

- [ ] **Step 4: 编写 search.wxss**

```css
/* pages/search/search.wxss */
.container {
  padding: 24rpx 32rpx;
}

.search-bar {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background-color: var(--bg-card);
  border-radius: 16rpx;
}
.search-icon {
  font-size: 28rpx;
  margin-right: 16rpx;
}
.search-input {
  flex: 1;
  color: var(--text-primary);
  font-size: 28rpx;
}
.clear-btn {
  font-size: 28rpx;
  color: var(--text-secondary);
  padding: 8rpx;
}

/* 热门搜索 */
.hot-section {
  margin-top: 40rpx;
}
.hot-title {
  font-size: 28rpx;
  color: var(--text-secondary);
  margin-bottom: 20rpx;
}
.hot-tags {
  display: flex;
  flex-wrap: wrap;
}
.hot-tag {
  padding: 12rpx 28rpx;
  margin: 0 16rpx 16rpx 0;
  background-color: var(--bg-card);
  color: var(--text-secondary);
  font-size: 26rpx;
  border-radius: 32rpx;
}

/* 搜索结果 */
.result-section {
  margin-top: 24rpx;
}
.result-count {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-bottom: 16rpx;
  display: block;
}
.result-item {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid var(--divider);
}
.result-image {
  width: 140rpx;
  height: 140rpx;
  border-radius: 8rpx;
  background-color: #1a1a2e;
  flex-shrink: 0;
}
.result-placeholder {
  width: 140rpx;
  height: 140rpx;
  border-radius: 8rpx;
  background-color: #1a1a2e;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  flex-shrink: 0;
}
.result-body {
  flex: 1;
  margin-left: 24rpx;
}
.result-name {
  font-size: 30rpx;
  color: var(--text-primary);
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
}
.result-brand {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-top: 8rpx;
  display: block;
}
.result-price {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--accent);
  margin-top: 8rpx;
  display: block;
}

.empty {
  text-align: center;
  padding: 120rpx 0;
  color: var(--text-secondary);
}
```

---

### Task 11: 产品详情页 pages/detail

**Files:**
- Create: `miniprogram/pages/detail/detail.js`
- Create: `miniprogram/pages/detail/detail.json`
- Create: `miniprogram/pages/detail/detail.wxml`
- Create: `miniprogram/pages/detail/detail.wxss`

- [ ] **Step 1: 编写 detail.json**

```json
{
  "usingComponents": {},
  "navigationBarTitleText": "产品详情"
}
```

- [ ] **Step 2: 编写 detail.js**

```js
// pages/detail/detail.js
const util = require('../../utils/util');
const db = wx.cloud.database();

Page({
  data: {
    product: null,
    wechatId: '',
    currentImageIndex: 0
  },

  onLoad(options) {
    const id = options.id;
    if (id) {
      this.loadProduct(id);
      this.loadConfig();
    }
  },

  async loadProduct(id) {
    try {
      const { data } = await db.collection('products').doc(id).get();
      this.setData({ product: data });

      wx.setNavigationBarTitle({
        title: data.name || '产品详情'
      });
    } catch (err) {
      wx.showToast({ title: '加载失败', icon: 'none' });
      setTimeout(() => wx.navigateBack(), 1500);
    }
  },

  async loadConfig() {
    const config = await util.fetchConfig(db, 'contact');
    if (config) {
      this.setData({ wechatId: config.wechatId });
    }
  },

  onSwiperChange(e) {
    this.setData({ currentImageIndex: e.detail.current });
  },

  onPreviewImage(e) {
    const index = e.currentTarget.dataset.index;
    const images = this.data.product.images;
    wx.previewImage({
      urls: images,
      current: images[index]
    });
  },

  onCopyWechat() {
    util.copyWechatId(this.data.wechatId);
  },

  onShareAppMessage() {
    const product = this.data.product;
    return {
      title: product ? product.name : '产品目录',
      path: `/pages/detail/detail?id=${product._id}`
    };
  }
});
```

- [ ] **Step 3: 编写 detail.wxml**

```html
<!-- pages/detail/detail.wxml -->
<view wx:if="{{product}}" class="container">

  <!-- 图片轮播 -->
  <swiper
    class="detail-swiper"
    indicator-dots
    indicator-color="#9e9e9e"
    indicator-active-color="#00e5ff"
    bindchange="onSwiperChange"
  >
    <block wx:if="{{product.images.length}}">
      <swiper-item
        wx:for="{{product.images}}"
        wx:key="*this"
        data-index="{{index}}"
        bindtap="onPreviewImage"
      >
        <image src="{{item}}" mode="aspectFit" class="detail-image" />
      </swiper-item>
    </block>
    <swiper-item wx:else>
      <view class="detail-placeholder">📦</view>
    </swiper-item>
  </swiper>

  <!-- 产品信息 -->
  <view class="info-section">
    <text class="product-name">{{product.name}}</text>
    <text class="product-price">¥{{product.price}}</text>
    <text class="product-meta">{{product.brand}} · {{product.model}} · {{product.category}}</text>
  </view>

  <!-- 产品描述 -->
  <view class="desc-section" wx:if="{{product.description}}">
    <text class="desc-text">{{product.description}}</text>
  </view>

  <!-- 规格表 -->
  <view class="specs-section" wx:if="{{product.specs.length}}">
    <text class="specs-title">📋 详细规格</text>
    <view class="specs-table">
      <view
        wx:for="{{product.specs}}"
        wx:key="key"
        class="spec-row"
      >
        <text class="spec-key">{{item.key}}</text>
        <text class="spec-value">{{item.value}}</text>
      </view>
    </view>
  </view>

</view>

<!-- 空白占位给底部固定按钮 -->
<view style="height: 120rpx;"></view>

<!-- 底部固定按钮 -->
<view wx:if="{{product}}" class="bottom-bar">
  <button class="copy-btn" bindtap="onCopyWechat">
    <text class="copy-icon">📋</text>
    <text>复制微信号</text>
  </button>
</view>
```

- [ ] **Step 4: 编写 detail.wxss**

```css
/* pages/detail/detail.wxss */
.container {
  padding-bottom: 40rpx;
}

.detail-swiper {
  width: 100%;
  height: 600rpx;
  background-color: #14141f;
}
.detail-image {
  width: 100%;
  height: 100%;
}
.detail-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 96rpx;
}

/* 产品信息 */
.info-section {
  padding: 32rpx;
}
.product-name {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--text-primary);
  display: block;
  line-height: 1.4;
}
.product-price {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--accent);
  display: block;
  margin-top: 16rpx;
}
.product-meta {
  font-size: 24rpx;
  color: var(--text-secondary);
  display: block;
  margin-top: 12rpx;
}

/* 描述 */
.desc-section {
  margin: 0 32rpx 24rpx;
  padding: 24rpx;
  background-color: var(--bg-card);
  border-radius: 8rpx;
}
.desc-text {
  color: var(--text-secondary);
  font-size: 28rpx;
  line-height: 1.6;
}

/* 规格表 */
.specs-section {
  margin: 0 32rpx;
}
.specs-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 16rpx;
  display: block;
}
.specs-table {
  background-color: var(--bg-card);
  border-radius: 8rpx;
  overflow: hidden;
}
.spec-row {
  display: flex;
  justify-content: space-between;
  padding: 20rpx 24rpx;
  border-bottom: 1rpx solid var(--divider);
}
.spec-row:last-child {
  border-bottom: none;
}
.spec-key {
  color: var(--text-secondary);
  font-size: 26rpx;
}
.spec-value {
  color: var(--text-primary);
  font-size: 26rpx;
}

/* 底部固定按钮 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 16rpx 32rpx;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
  background-color: var(--bg-primary);
  border-top: 1rpx solid var(--divider);
}
.copy-btn {
  width: 100%;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: var(--accent);
  color: #0a0a0f;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 16rpx;
  border: none;
}
.copy-btn::after {
  border: none;
}
.copy-icon {
  margin-right: 12rpx;
}
```

---

### Task 12: 关于页 pages/about

**Files:**
- Create: `miniprogram/pages/about/about.js`
- Create: `miniprogram/pages/about/about.json`
- Create: `miniprogram/pages/about/about.wxml`
- Create: `miniprogram/pages/about/about.wxss`

- [ ] **Step 1: 编写 about.json**

```json
{
  "usingComponents": {},
  "navigationBarTitleText": "关于"
}
```

- [ ] **Step 2: 编写 about.js**

```js
// pages/about/about.js
const util = require('../../utils/util');
const db = wx.cloud.database();

Page({
  data: {
    productCount: 0,
    wechatId: '',
    brandName: '产品目录'
  },

  onLoad() {
    this.loadData();
    this.loadConfig();
  },

  async loadData() {
    try {
      const res = await db.collection('products').count();
      this.setData({ productCount: res.total });
    } catch (err) {
      // ignore
    }
  },

  async loadConfig() {
    const config = await util.fetchConfig(db, 'contact');
    if (config) {
      this.setData({ wechatId: config.wechatId });
    }
  },

  onCopyWechat() {
    util.copyWechatId(this.data.wechatId);
  }
});
```

- [ ] **Step 3: 编写 about.wxml**

```html
<!-- pages/about/about.wxml -->
<view class="container">
  <view class="brand-section">
    <text class="brand-name">{{brandName}}</text>
    <text class="brand-desc">科技消费品展示目录</text>
  </view>

  <view class="stat-section">
    <view class="stat-item">
      <text class="stat-number">{{productCount}}</text>
      <text class="stat-label">款产品</text>
    </view>
  </view>

  <view class="contact-section">
    <text class="contact-title">联系我们</text>
    <view class="contact-card" bindtap="onCopyWechat">
      <text class="contact-label">微信号</text>
      <text class="contact-value">{{wechatId || '未设置'}}</text>
      <text class="contact-hint">点击复制</text>
    </view>
  </view>

  <view class="footer">
    <text class="footer-text">Powered by WeChat CloudBase</text>
  </view>
</view>
```

- [ ] **Step 4: 编写 about.wxss**

```css
/* pages/about/about.wxss */
.container {
  padding: 64rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.brand-section {
  text-align: center;
  margin-bottom: 48rpx;
}
.brand-name {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text-primary);
  display: block;
}
.brand-desc {
  font-size: 26rpx;
  color: var(--text-secondary);
  margin-top: 12rpx;
  display: block;
}

.stat-section {
  display: flex;
  justify-content: center;
  margin-bottom: 48rpx;
}
.stat-item {
  text-align: center;
  padding: 32rpx 64rpx;
  background-color: var(--bg-card);
  border-radius: 12rpx;
}
.stat-number {
  font-size: 48rpx;
  font-weight: 700;
  color: var(--accent);
  display: block;
}
.stat-label {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-top: 8rpx;
  display: block;
}

.contact-section {
  width: 100%;
  margin-bottom: 48rpx;
}
.contact-title {
  font-size: 28rpx;
  color: var(--text-secondary);
  margin-bottom: 16rpx;
  display: block;
}
.contact-card {
  background-color: var(--bg-card);
  padding: 28rpx 24rpx;
  border-radius: 12rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.contact-label {
  font-size: 22rpx;
  color: var(--text-secondary);
}
.contact-value {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-top: 8rpx;
}
.contact-hint {
  font-size: 22rpx;
  color: var(--accent);
  margin-top: 8rpx;
}

.footer {
  margin-top: 40rpx;
}
.footer-text {
  font-size: 22rpx;
  color: var(--text-secondary);
}
```

---

### Task 13: 注册审核与发布

- [ ] **Step 1: 准备审核材料**

- 选类目：「工具 > 信息查询」
- 准备 3 张小程序截图（首页、详情页、分类页）

- [ ] **Step 2: 上传代码**

微信开发者工具 → 上传 → 填写版本号 `1.0.0`，备注「首次提交：产品展示目录」。

- [ ] **Step 3: 提交审核**

小程序后台 → 版本管理 → 选择刚上传的版本 → 提交审核。

- [ ] **Step 4: 审核通过后发布**

审核通过（通常 1-3 天）→ 点击发布，小程序即上线。

---

### Task 14: 产品数据填充

- [ ] **Step 1: 上传产品图片**

云开发控制台 → 存储 → 上传产品图片。获取每张图片的 `fileID`（形如 `cloud://product-catalog.xxx.png`）。

- [ ] **Step 2: 更新产品记录**

在 `products` 集合中，为每条产品填充 `images` 数组：
```json
{
  "images": [
    "cloud://product-catalog.7072-product-catalog-123456/headphones-1.png",
    "cloud://product-catalog.7072-product-catalog-123456/headphones-2.png"
  ]
}
```

- [ ] **Step 3: 批量导入产品**

使用云开发控制台的数据导入功能（支持 JSON/CSV），一次性导入剩余 47+ 条产品数据。

- [ ] **Step 4: 更新 config 微信号**

在 `config` 集合中将 `contact` 记录改为实际微信号。

- [ ] **Step 5: 验证**

真机扫码测试所有页面，确认图片显示正常，复制微信号功能可用。

---
